// Placeholder package for cross-cutting types shared between apps/api and
// apps/web (e.g. the normalized ResourceSignal type from SPEC.md section 10).
// Intentionally empty until connectors/verification-engine work begins.
export {};
