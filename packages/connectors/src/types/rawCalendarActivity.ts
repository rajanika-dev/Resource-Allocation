/** Shape of a single row in demo-data/calendar.json (fake "Calendar-like activity source"). */
export interface RawCalendarActivity {
  attendeeEmail: string;
  projectLabel: string;
  meetingHours: number;
  week: string;
}
